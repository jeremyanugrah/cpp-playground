# TODO

> **Last updated:** February 13, 2026

## Neovim

- [ ] Add `compinit` + menu select to `.zshrc` for tab-completion (git, apt, etc.)
- [ ] Guard `WAYLAND_DISPLAY` with fallback: `export WAYLAND_DISPLAY="${WAYLAND_DISPLAY:-wayland-0}"`
- [ ] Consider Makefile or multi-file compilation when projects grow beyond single files
- [ ] Explore `nvim-dap` step-over/step-into keybindings (`dn`, `di`, `do`)

## Projects

- [ ] Add more C++ projects beyond hello-world
- [ ] Set up Python virtual environment in python-playground
- [ ] Try debugging a multi-file C++ project with codelldb

## Environment

- [ ] Test SSH into Crostini from another device (Nerd Font icon fallback)
- [ ] Backup dotfiles (init.lua, foot.ini, .zshrc) to a separate repo or dotfiles folder
